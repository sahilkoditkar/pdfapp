# ML Kit Document Scanner: scanner UI and model live in Google Play Services,
# so the client-side library needs no extra keep rules at the moment. Add
# rules here if R8 starts stripping required classes after a library upgrade.
